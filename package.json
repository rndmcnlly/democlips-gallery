{
  "name": "democlips-gallery",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create democlips-gallery",
    "db:migrate": "wrangler d1 execute democlips-gallery --file=schema.sql",
    "db:migrate:local": "wrangler d1 execute democlips-gallery --local --file=schema.sql"
  },
  "dependencies": {
    "hono": "^4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4",
    "typescript": "^5",
    "wrangler": "^4.64.0"
  }
}
